const initialState = {
    accuracy: null,
    batches: [],
    lossData: [],
    accuracyData: [],
    batchGraphData: [],
    lastBatchNum: 0
}

export default function batchGraphReducer(state = initialState, action) {
    switch (action.type) {
        default:
            return state
    }
}